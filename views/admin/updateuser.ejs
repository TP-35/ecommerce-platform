<html lang="en">

<%- include('../partials/head.ejs', {title: "Update User" , css: "/css/adminpanel.css" }) %>

    <body>
        <%- include('../partials/nav.ejs') %>

            <div class="container">

                <%- include('../partials/sidebar.ejs') %>

                    <form method="POST" action="/users/<%=user.username%>">
                        <table class="table">
                            <!-- Sets header for table contents -->
                            <thead>
                                <tr>
                                    <th scope="col">User ID</th>
                                    <th scope="col">Username</th>
                                    <th scope="col">Password</th>
                                    <th scope="col">Email</th>
                                    <th scope="col">Role ID</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>
                                        <%= user.user_id%>
                                    </td>
                                    <td>
                                        <input id="username" type="text" name="username" value="<%=user.username%>"></input>
                                    </td>
                                    <td>
                                        <input id="password" type="password" name="password" value=""></input>
                                    </td>
                                    <td>
                                        <input id="email" type="email" name="email" value="<%=user.email%>"></input>
                                    </td>
                                    <td>
                                        <input id="role" type="text" name="role" value="<%=user.role_id%>"></input>
                                    </td>
                                </tr>
                                    <table>
                                        <%= user.message %>
                                    </table>
                            </tbody>
                        </table>
                        <!-- Calls a function that updates the database with changed records -->
                        <button class="submit" style="float: right;" onclick="addUser()">Submit
                            Changes</button>
                    </form>
            </div>

            <script src="/js/adminpanel.js" type="text/javascript"></script>
            <%- include('../partials/footer.ejs') %>

    </body>

</html>